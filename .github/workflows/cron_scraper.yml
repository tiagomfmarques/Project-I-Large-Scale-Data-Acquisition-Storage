name: Automacao Diaria de Scraping

on:
  schedule:
    - cron: '0 20 * * *'
  workflow_dispatch:

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout do repositorio
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Executar Pipeline
        env:

          PYTHONPATH: ${{ github.workspace }}
        run: |
          python src/scrapers/run_pipeline.py

      - name: Fazer Commit dos Dados e Logs
        run: |
          git config --global user.name "GitHub Action Scraper"
          git config --global user.email "action@github.com"
          # Adiciona as pastas para garantir que novos ficheiros sao detetados
          git add data/raw/scientific_articles_data.json
          git add logs/logs.log
          git commit -m "Auto-update: Novos artigos [$(date +'%Y-%m-%d')]" || echo "Sem alteracoes para commitar"
          git push